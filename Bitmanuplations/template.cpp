#include <bits/stdc++.h>
using namespace std;


/**
 * @brief 
 * solutions is we have to make two set having the 
 * 
 */

void solve(vector<int>arr){
    
}

int main(){
   int n;
   vector<int>arr(n);
   for(int i=0; i<n; i++)
   {
        cin>>arr[i];
   } 
   solve(arr);
}